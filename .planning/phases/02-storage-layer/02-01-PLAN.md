---
phase: 02-storage-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/unit/test_storage.py
  - src/carpix_images/services/__init__.py
  - src/carpix_images/services/storage.py
autonomous: true
requirements:
  - STORE-01
  - STORE-02

must_haves:
  truths:
    - "pytest collects 5 tests from tests/unit/test_storage.py and all 5 FAIL (RED)"
    - "StorageService can be imported from carpix_images.services.storage without ModuleNotFoundError"
    - "Full test suite collects without error (existing 11 tests still GREEN)"
  artifacts:
    - path: "src/carpix_images/services/__init__.py"
      provides: "Python package marker for services/ subdirectory"
    - path: "src/carpix_images/services/storage.py"
      provides: "StorageService stub class (raises NotImplementedError)"
    - path: "tests/unit/test_storage.py"
      provides: "5 failing unit tests for STORE-01 and STORE-02"
    - path: "tests/conftest.py"
      provides: "IMAGES_DIR env guard added alongside existing DATABASE_URL guard"
  key_links:
    - from: "tests/unit/test_storage.py"
      to: "src/carpix_images/services/storage.py"
      via: "from carpix_images.services.storage import StorageService"
      pattern: "from carpix_images\\.services\\.storage import StorageService"
---

<objective>
Establish the TDD RED baseline for Phase 2: create the services package marker, a
StorageService stub that fails all tests, and 5 unit tests that define the exact contract
Plan 02 must satisfy.

Purpose: Plan 02 (Wave 2) needs a RED test suite to implement against. This plan creates
the failing baseline so Plan 02 can go from RED to GREEN without ambiguity.

Output: 5 collected-but-failing tests, an importable stub, and the IMAGES_DIR env guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-storage-layer/02-RESEARCH.md
@.planning/phases/02-storage-layer/02-PATTERNS.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Phase 1 patterns the executor must replicate. Extracted from actual Phase 1 files. -->

From tests/conftest.py (current full content — 6 lines):
  import os
  # Must run before any carpix_images module — Settings() reads env at import time.
  os.environ.setdefault(
      "DATABASE_URL", "postgresql+asyncpg://user:pass@localhost:5432/testdb"
  )

From tests/unit/test_normalize.py (class-based test pattern):
  class TestCanonicalKey:
      def test_output_is_lowercase(self) -> None: ...
      def test_dm_i_variants_are_equivalent(self) -> None: ...

  All test methods annotated -> None. asyncio_mode="auto" in pyproject.toml
  means async test methods need NO @pytest.mark.asyncio decorator.

From src/carpix_images/domain/__init__.py:
  (empty file — exact pattern for services/__init__.py)

From src/carpix_images/domain/normalize.py line 1:
  from __future__ import annotations
  (all new modules use this header)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create services package + StorageService stub</name>
  <files>src/carpix_images/services/__init__.py, src/carpix_images/services/storage.py</files>

  <read_first>
    - src/carpix_images/domain/__init__.py (empty package marker pattern — replicate exactly)
    - src/carpix_images/domain/normalize.py (from __future__ import annotations header + module style)
    - .planning/phases/02-storage-layer/02-PATTERNS.md (full StorageService skeleton and constraints)
    - .planning/phases/02-storage-layer/02-RESEARCH.md section "Complete StorageService skeleton"
  </read_first>

  <action>
    Create src/carpix_images/services/__init__.py as a completely empty file (zero bytes).
    This is the Python package marker — do NOT add any imports or comments.

    Create src/carpix_images/services/storage.py as a stub:
    - Start with `from __future__ import annotations`
    - Import Path from pathlib, anyio, and FileResponse from fastapi.responses
    - Define class StorageService with __init__(self, base_dir: Path) -> None
    - In __init__, assign self._base = base_dir.resolve()
    - Define async def save(self, brand_key: str, model_key: str, year: int, data: bytes) -> Path
      with body: raise NotImplementedError
    - Define def _validated_path(self, brand_key: str, model_key: str, year: int) -> Path
      with body: raise NotImplementedError
    - Define def file_response(self, brand_key: str, model_key: str, year: int) -> FileResponse
      with body: raise NotImplementedError

    The stub must be importable (no syntax errors) and all methods must raise NotImplementedError
    so the Wave 0 tests fail with NotImplementedError, not ImportError.

    ruff constraints: use str | None not Optional[str], Path not os.PathLike, no typing imports.
    mypy --strict constraint: all methods need complete type annotations (already satisfied by
    the signatures above).
  </action>

  <verify>
    <automated>cd /home/ccastro/Projects/carpix && python3 -c "from carpix_images.services.storage import StorageService; s = StorageService.__new__(StorageService); print('import OK')"</automated>
  </verify>

  <acceptance_criteria>
    - src/carpix_images/services/__init__.py exists and is empty (0 bytes)
    - src/carpix_images/services/storage.py exists with class StorageService:
    - StorageService.__init__ signature: (self, base_dir: Path) -> None
    - StorageService.save signature: async def save(self, brand_key: str, model_key: str, year: int, data: bytes) -> Path
    - StorageService.file_response signature: def file_response(self, brand_key: str, model_key: str, year: int) -> FileResponse
    - `python3 -c "from carpix_images.services.storage import StorageService"` exits 0 with no output
    - `python3 -m ruff check src/carpix_images/services/` exits 0
    - `python3 -m mypy src/carpix_images/services/storage.py` exits 0
  </acceptance_criteria>

  <done>services/__init__.py (empty) and storage.py (importable stub with NotImplementedError bodies) committed and passing ruff + mypy.</done>
</task>

<task type="tdd">
  <name>Task 2: Write 5 failing unit tests for StorageService (RED baseline)</name>
  <files>tests/unit/test_storage.py, tests/conftest.py</files>

  <read_first>
    - tests/conftest.py (current content — append IMAGES_DIR line after existing DATABASE_URL block)
    - tests/unit/test_normalize.py (class-based + -> None annotation pattern)
    - tests/unit/test_health.py (async test method pattern without @pytest.mark.asyncio)
    - .planning/phases/02-storage-layer/02-PATTERNS.md section "tests/unit/test_storage.py (create)"
    - .planning/phases/02-storage-layer/02-RESEARCH.md section "Unit test skeleton (for Wave 0 stubs)"
  </read_first>

  <action>
    Update tests/conftest.py: append one line after the existing setdefault block:
      os.environ.setdefault("IMAGES_DIR", "/tmp/carpix_test_images")
    The comment at the top of conftest.py ("Must run before any carpix_images module") must
    be preserved exactly as-is.

    Create tests/unit/test_storage.py with the following structure:
    - `from __future__ import annotations` header (line 1)
    - Imports: from pathlib import Path; import pytest; from carpix_images.services.storage import StorageService
    - Class TestStorageServiceSave (covers STORE-01):
        async def test_save_writes_file_at_correct_path(self, tmp_path: Path) -> None
          Instantiates StorageService(tmp_path), awaits save("toyota", "camry", 2020, b"FAKE_JPEG"),
          asserts (tmp_path / "toyota" / "camry" / "2020" / "image.jpg").exists() is True,
          asserts read_bytes() == b"FAKE_JPEG"
        async def test_save_creates_intermediate_directories(self, tmp_path: Path) -> None
          Instantiates StorageService(tmp_path), awaits save("byddmi", "sealudmi", 2023, b"DATA"),
          asserts (tmp_path / "byddmi" / "sealudmi" / "2023").is_dir() is True
        async def test_save_is_idempotent(self, tmp_path: Path) -> None
          Saves b"FIRST" then b"SECOND" for same key, asserts final read_bytes() == b"SECOND"
    - Class TestStorageServiceFileResponse (covers STORE-02):
        def test_valid_path_returns_file_response(self, tmp_path: Path) -> None
          Creates directory tmp_path / "toyota" / "camry" / "2020" via mkdir(parents=True),
          writes b"DATA" to that dir / "image.jpg", instantiates StorageService(tmp_path),
          calls file_response("toyota", "camry", 2020), imports FileResponse from
          fastapi.responses and asserts isinstance(resp, FileResponse)
        def test_traversal_attempt_raises_value_error(self, tmp_path: Path) -> None
          Instantiates StorageService(tmp_path), uses pytest.raises(ValueError, match="traversal")
          block, calls svc.file_response("..", "..", 2020) inside the block

    No @pytest.mark.asyncio decorators — asyncio_mode="auto" in pyproject.toml handles this.
    All test methods annotated -> None.
    The import of FileResponse inside test_valid_path_returns_file_response should be a
    top-level import (not inside the method) to follow ruff I001 import ordering rules.
  </action>

  <verify>
    <automated>cd /home/ccastro/Projects/carpix && python3 -m pytest tests/unit/test_storage.py --collect-only -q 2>&1 | tail -5</automated>
    <automated>cd /home/ccastro/Projects/carpix && python3 -m pytest tests/unit/test_storage.py -x -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - tests/unit/test_storage.py exists
    - `python3 -m pytest tests/unit/test_storage.py --collect-only -q` exits 0 and shows exactly 5 tests collected
    - `python3 -m pytest tests/unit/test_storage.py -x -q` exits NON-ZERO with NotImplementedError (RED — all 5 tests failing)
    - `python3 -m pytest tests/ --collect-only -q` exits 0 with existing 11 tests still collected (total 16 tests collected)
    - `python3 -m ruff check tests/unit/test_storage.py` exits 0
    - `python3 -m mypy tests/unit/test_storage.py` exits 0
    - tests/conftest.py contains: os.environ.setdefault("IMAGES_DIR", "/tmp/carpix_test_images")
    - Existing DATABASE_URL line in conftest.py is unchanged
  </acceptance_criteria>

  <done>5 failing unit tests collected without collection errors, 11 pre-existing tests still GREEN, conftest.py updated with IMAGES_DIR guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → StorageService stub | Only test inputs during Wave 0; no untrusted data crosses in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 introduces no new packages — anyio and starlette are existing transitive deps; no install commands in this plan |

Note: Path traversal threat (T-02-01) is addressed in Plan 02 where the actual guard is
implemented. Wave 0 stubs raise NotImplementedError unconditionally — no security-sensitive
logic is present in this plan.
</threat_model>

<verification>
Wave 0 complete when:
- `python3 -m pytest tests/unit/test_storage.py --collect-only -q` exits 0 with 5 tests listed
- `python3 -m pytest tests/unit/test_storage.py -q` exits NON-ZERO (all 5 RED)
- `python3 -m pytest tests/ --collect-only -q` exits 0 (16 total tests, no collection errors)
- `python3 -m ruff check src/carpix_images/services/ tests/unit/test_storage.py` exits 0
- `python3 -m mypy src/carpix_images/services/storage.py tests/unit/test_storage.py` exits 0
</verification>

<success_criteria>
- StorageService importable from carpix_images.services.storage without error
- 5 unit tests collected and failing (NotImplementedError from stubs, not ImportError)
- Pre-existing 11 tests unaffected and still GREEN
- ruff and mypy clean on all new files
</success_criteria>

<output>
Create `.planning/phases/02-storage-layer/02-01-SUMMARY.md` when done.
</output>

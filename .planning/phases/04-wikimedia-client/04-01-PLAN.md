---
phase: 04-wikimedia-client
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/carpix_images/services/wikimedia.py
  - tests/unit/test_wikimedia.py
autonomous: true
requirements:
  - WIKI-01
  - WIKI-02
  - WIKI-03

must_haves:
  truths:
    - "httpx is declared in [project].dependencies so the app can import WikimediaClient without dev extras"
    - "tests/unit/test_wikimedia.py exists with 6 tests, all FAILING (RED) because wikimedia.py has stub NotImplementedError"
    - "The test file covers all six behaviors: thumburl return, SVG skip, PNG skip, mixed-type JPEG selection, fallback query trigger, and None sentinel"
  artifacts:
    - path: "src/carpix_images/services/wikimedia.py"
      provides: "WikimediaClient stub — class exists, find_jpeg_url and _search_first_jpeg raise NotImplementedError"
      contains: "class WikimediaClient"
    - path: "tests/unit/test_wikimedia.py"
      provides: "6 failing unit tests covering WIKI-01, WIKI-02, WIKI-03"
      exports: []
  key_links:
    - from: "tests/unit/test_wikimedia.py"
      to: "src/carpix_images/services/wikimedia.py"
      via: "import WikimediaClient"
      pattern: "from carpix_images.services.wikimedia import WikimediaClient"
---

<objective>
TDD RED baseline for WikimediaClient: move httpx to production dependencies, create a stub implementation, and write 6 failing unit tests that define the exact behaviors required by WIKI-01, WIKI-02, and WIKI-03.

Purpose: Tests written before implementation lock in the contract. The stub raises NotImplementedError, guaranteeing all 6 tests fail (RED). Wave 2 implements the client to turn them GREEN.
Output: pyproject.toml updated, wikimedia.py stub, test_wikimedia.py with 6 RED tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-wikimedia-client/04-RESEARCH.md
@.planning/phases/04-wikimedia-client/04-PATTERNS.md

<interfaces>
<!-- Existing patterns the executor must replicate -->

From src/carpix_images/infrastructure/cache_repository.py:
  class CacheRepository:
      def __init__(self, engine: AsyncEngine) -> None:
          self._engine = engine
      async def find(self, brand_key: str, model_key: str, year: int) -> CacheEntry | None: ...
      async def insert(self, ...) -> None: ...

From src/carpix_images/services/storage.py:
  class StorageService:
      def __init__(self, base_dir: Path) -> None: ...
      async def save(self, brand_key: str, model_key: str, year: int, data: bytes) -> Path: ...
      def file_response(self, brand_key: str, model_key: str, year: int) -> FileResponse: ...

From tests/unit/test_storage.py (test style):
  asyncio_mode = "auto" -- no @pytest.mark.asyncio decorator on async tests
  class-grouped tests or module-level fixtures accepted
  pytest.fixture() used for shared setup

pyproject.toml [project].dependencies (current):
  "fastapi>=0.136.1", "uvicorn[standard]>=0.47.0", "pydantic-settings>=2.14.1",
  "asyncpg>=0.31.0", "sqlalchemy>=2.0"

pyproject.toml [project.optional-dependencies].dev (current):
  "httpx>=0.28.1" -- MUST be moved to [project].dependencies
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move httpx to prod deps and create WikimediaClient stub</name>
  <files>pyproject.toml, src/carpix_images/services/wikimedia.py</files>
  <read_first>
    - pyproject.toml (current [project].dependencies and [project.optional-dependencies].dev blocks)
    - src/carpix_images/services/storage.py (DI constructor and module header pattern to replicate)
    - src/carpix_images/infrastructure/cache_repository.py (async method shape and str | None sentinel pattern)
  </read_first>
  <behavior>
    - After the change, "httpx>=0.28.1" appears in [project].dependencies and is absent from [project.optional-dependencies].dev
    - wikimedia.py opens with "from __future__ import annotations"
    - Module-level constants: _COMMONS_API = "https://commons.wikimedia.org/w/api.php" and _USER_AGENT = "carpix-images/0.1 (https://github.com/user/carpix)"
    - WikimediaClient.__init__(self, client: httpx.AsyncClient) -> None stores self._client = client
    - find_jpeg_url(self, brand: str, model: str, year: int) -> str | None raises NotImplementedError
    - _search_first_jpeg(self, query: str) -> str | None raises NotImplementedError
    - File passes "ruff check src/carpix_images/services/wikimedia.py" (no ruff errors on the stub)
    - File passes "mypy --strict src/carpix_images/services/wikimedia.py" (stub uses correct type annotations)
  </behavior>
  <action>
    In pyproject.toml: add "httpx>=0.28.1" as the last entry in [project].dependencies. Remove "httpx>=0.28.1" from [project.optional-dependencies].dev. Do not change any other entry.

    Create src/carpix_images/services/wikimedia.py following the cache_repository.py pattern exactly:
    - First line: from __future__ import annotations
    - Import block: import httpx (only; no unused imports)
    - Two module-level string constants: _COMMONS_API and _USER_AGENT (values specified in behavior above)
    - WikimediaClient class with __init__ storing self._client
    - find_jpeg_url async method with return type str | None, body: raise NotImplementedError
    - _search_first_jpeg async method with return type str | None, body: raise NotImplementedError
    - No docstrings, no comments (WHY is not non-obvious), no blank lines between raise statements and return type

    No uv sync needed — the executor's environment already has httpx installed via dev extras. The pyproject.toml change is for correctness of the production dependency declaration.
  </action>
  <verify>
    <automated>cd /home/ccastro/Projects/carpix && python -m ruff check src/carpix_images/services/wikimedia.py && python -m mypy --strict src/carpix_images/services/wikimedia.py && python -c "import carpix_images.services.wikimedia; print('import OK')" && grep -n "httpx" pyproject.toml</automated>
  </verify>
  <done>
    - wikimedia.py imports cleanly from src package
    - ruff and mypy --strict report zero errors on wikimedia.py
    - grep shows "httpx>=0.28.1" appears exactly once in pyproject.toml under [project].dependencies and zero times under [project.optional-dependencies].dev
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write 6 failing unit tests (RED baseline)</name>
  <files>tests/unit/test_wikimedia.py</files>
  <read_first>
    - tests/unit/test_storage.py (exact test file structure, fixture pattern, async test style to replicate)
    - src/carpix_images/services/wikimedia.py (just-created stub — verify the import path and method signatures)
    - .planning/phases/04-wikimedia-client/04-RESEARCH.md (Pattern 3, Pattern 4, Pattern 5 — respx mock shapes; Validation Architecture section for exact test names)
  </read_first>
  <behavior>
    - Test 1 (WIKI-01): test_find_jpeg_url_returns_thumburl_exactly — respx returns a single JPEG page; assert result equals the exact thumburl string "https://upload.wikimedia.org/wikipedia/commons/thumb/a/ab/Toyota_Corolla_2022.jpg/960px-Toyota_Corolla_2022.jpg"
    - Test 2 (WIKI-02a): test_skips_svg_and_returns_none — respx returns one page with mime "image/svg+xml"; assert result is None
    - Test 3 (WIKI-02b): test_skips_png_and_returns_none — respx returns one page with mime "image/png"; assert result is None
    - Test 4 (WIKI-02c): test_selects_jpeg_from_mixed_types — respx returns two pages: index=1 has mime "image/svg+xml", index=2 has mime "image/jpeg" with thumburl "https://upload.wikimedia.org/jpeg.jpg"; assert result == "https://upload.wikimedia.org/jpeg.jpg"
    - Test 5 (WIKI-03a): test_fallback_query_called_when_primary_returns_no_jpeg — first call (primary) returns SVG page; second call (fallback) returns JPEG page with thumburl "https://upload.wikimedia.org/fallback.jpg"; assert result == "https://upload.wikimedia.org/fallback.jpg"; assert respx_mock.calls.call_count == 2
    - Test 6 (WIKI-03b): test_returns_none_when_both_queries_yield_no_jpeg — respx returns empty pages dict {}; assert result is None; assert respx_mock.calls.call_count == 2
    - All 6 tests FAIL with NotImplementedError when run against the stub
    - File passes ruff check (no lint errors)
  </behavior>
  <action>
    Create tests/unit/test_wikimedia.py:

    Module docstring: """Unit tests for WikimediaClient — covers WIKI-01, WIKI-02, WIKI-03."""

    Imports (in ruff I-sorted order): from __future__ import annotations, then import httpx, import pytest, import respx, then from carpix_images.services.wikimedia import WikimediaClient.

    Module-level fixture (not inside a class):
      @pytest.fixture()
      def wiki_client() -> WikimediaClient:
          return WikimediaClient(httpx.AsyncClient())

    Six async test functions (no class grouping, no @pytest.mark.asyncio — asyncio_mode="auto"):
    - Each test takes (wiki_client: WikimediaClient, respx_mock: respx.MockRouter) parameters
    - respx_mock.get("https://commons.wikimedia.org/w/api.php").respond(200, json={...}) registers mock before awaiting wiki_client methods
    - For tests 5 and 6: two sequential GET calls go to the same URL; use respx_mock.get(...).mock(side_effect=[response_1, response_2]) to return different responses on call 1 vs call 2, OR register the route with assert_all_called=False via @pytest.mark.respx(assert_all_called=False) on those two tests and use a single route that returns the appropriate per-call response
    - For test 5 specifically: primary returns {"query": {"pages": {"1": {"index": 1, "imageinfo": [{"mime": "image/svg+xml", "thumburl": "..."}]}}}}, fallback returns {"query": {"pages": {"2": {"index": 1, "imageinfo": [{"mime": "image/jpeg", "thumburl": "https://upload.wikimedia.org/fallback.jpg"}]}}}}
    - For test 6: both calls return {"query": {"pages": {}}}

    The respx side_effect for sequential responses: pass a list of httpx.Response objects to side_effect. Construct responses as httpx.Response(200, json={...}).

    All assertions must be deterministic string equality or is None checks — no "in" substring checks.
  </action>
  <verify>
    <automated>cd /home/ccastro/Projects/carpix && python -m ruff check tests/unit/test_wikimedia.py && python -m pytest tests/unit/test_wikimedia.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ruff check reports zero errors on test_wikimedia.py
    - pytest output shows exactly 6 tests collected, all FAILED (not ERROR), with NotImplementedError as the failure cause
    - No import errors or collection errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → stub | Test imports WikimediaClient stub; trust boundary is import resolution (package must be importable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | pyproject.toml dep move | accept | Change is mechanical (move one string between lists); ruff/mypy validate the stub immediately after |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan — httpx is already present in dev extras; only the declaration moves |
</threat_model>

<verification>
All 6 tests collected and FAILING with NotImplementedError (not ImportError, not collection error):
  python -m pytest tests/unit/test_wikimedia.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"
Expected: 6 lines containing FAILED, 0 containing PASSED or ERROR

httpx in correct pyproject.toml location:
  grep -n "httpx" pyproject.toml
Expected: one match under [project] dependencies block, zero matches under [project.optional-dependencies]
</verification>

<success_criteria>
- pyproject.toml: "httpx>=0.28.1" in [project].dependencies, absent from dev extras
- wikimedia.py: class WikimediaClient with find_jpeg_url and _search_first_jpeg stubs; ruff + mypy --strict clean
- test_wikimedia.py: 6 tests, all RED (NotImplementedError), ruff clean
- python -m pytest tests/unit/test_wikimedia.py exits non-zero (failures expected)
- python -m pytest tests/unit/ passes for all non-wikimedia tests (no regression)
</success_criteria>

<output>
Create .planning/phases/04-wikimedia-client/04-01-SUMMARY.md when done
</output>

---
phase: 05-service-orchestration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/carpix_images/services/image_service.py
  - src/carpix_images/services/__init__.py
  - tests/unit/test_image_service.py
autonomous: true
requirements: [CACHE-01, CACHE-02, CACHE-03, CACHE-04]

must_haves:
  truths:
    - "5 unit tests for ImageService exist and all fail (RED baseline)"
    - "ImageService class stub exists with get_or_fetch method raising NotImplementedError"
    - "ImageService is importable from carpix_images.services"
    - "Test file covers: cache-hit (no Wikimedia call), cache-miss (full fetch), concurrent serialization (one fetch), self-healing (file missing), no-image-found (404)"
  artifacts:
    - path: "src/carpix_images/services/image_service.py"
      provides: "ImageService stub"
      contains: "class ImageService"
    - path: "tests/unit/test_image_service.py"
      provides: "5 failing RED tests"
      contains: "test_cache_hit_returns_file_response_without_wikimedia_call"
  key_links:
    - from: "tests/unit/test_image_service.py"
      to: "src/carpix_images/services/image_service.py"
      via: "import ImageService"
      pattern: "from carpix_images.services.image_service import ImageService"
---

<objective>
Establish the TDD RED baseline for ImageService: stub class with get_or_fetch raising NotImplementedError, plus 5 failing tests that specify exactly what the implementation must do.

Purpose: Force the implementation in Plan 02 to be test-driven. Tests encode all four cache behaviors (hit, miss, concurrent, self-healing) before any implementation exists.
Output: image_service.py stub, 5 failing unit tests, services __init__.py updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-wikimedia-client/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/carpix_images/services/storage.py:
StorageService(base_dir: Path)
  async def save(brand_key: str, model_key: str, year: int, data: bytes) -> Path
  def file_response(brand_key: str, model_key: str, year: int) -> FileResponse

From src/carpix_images/infrastructure/cache_repository.py:
CacheRepository(engine: AsyncEngine)
  async def find(brand_key: str, model_key: str, year: int) -> CacheEntry | None
  async def insert(brand_key: str, model_key: str, year: int, local_path: str, source_url: str, file_title: str) -> None

@dataclass CacheEntry:
  brand_key: str, model_key: str, year: int,
  local_path: str, source_url: str, file_title: str, cached_at: datetime

From src/carpix_images/services/wikimedia.py:
WikimediaClient(client: httpx.AsyncClient)
  async def find_jpeg_url(brand: str, model: str, year: int) -> str | None

From src/carpix_images/domain/normalize.py:
  def canonical_key(value: str) -> str  # lowercase, strip non-alphanumeric
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Scaffold ImageService stub and RED test baseline</name>
  <files>
    src/carpix_images/services/image_service.py,
    src/carpix_images/services/__init__.py,
    tests/unit/test_image_service.py
  </files>
  <read_first>
    - src/carpix_images/services/storage.py (DI constructor pattern, method signatures)
    - src/carpix_images/services/wikimedia.py (DI constructor + async method signatures to replicate)
    - tests/unit/test_wikimedia.py (unit test structure: module-level fixtures, respx_mock, async def tests, asyncio_mode=auto, no @pytest.mark.asyncio)
    - tests/unit/test_storage.py (class vs module-level test organisation choice)
    - src/carpix_images/services/__init__.py (current contents — add ImageService export)
    - src/carpix_images/infrastructure/cache_repository.py (CacheEntry dataclass, CacheRepository interface)
    - src/carpix_images/domain/normalize.py (canonical_key signature)
  </read_first>
  <action>
    Create src/carpix_images/services/image_service.py with the ImageService stub class.
    The class takes three constructor parameters: storage (StorageService), repo (CacheRepository), wikimedia (WikimediaClient).
    Store each as self._storage, self._repo, self._wikimedia.
    Declare one public async method: get_or_fetch(brand: str, model: str, year: int) -> FileResponse.
    The method body raises NotImplementedError.
    The class also needs a dict[tuple[str, str, int], asyncio.Lock] attribute self._locks initialized to an empty dict — declare it in __init__ for mypy compliance but do not implement locking logic yet.
    Include from __future__ import annotations at the top. Import asyncio. Import FileResponse from fastapi.responses. Import StorageService from carpix_images.services.storage. Import CacheRepository from carpix_images.infrastructure.cache_repository. Import WikimediaClient from carpix_images.services.wikimedia.

    Update src/carpix_images/services/__init__.py to import and re-export ImageService from .image_service.

    Create tests/unit/test_image_service.py with exactly 5 async test functions (no test class wrapping — module-level, matching the flat style of test_wikimedia.py):

    Test 1 — test_cache_hit_returns_file_response_without_wikimedia_call:
      Arrange: mock repo.find to return a CacheEntry for ("toyota", "corolla", 2022); mock storage.file_response to return a FileResponse; create ImageService with these mocks.
      Act: call await svc.get_or_fetch("toyota", "corolla", 2022).
      Assert: the result is a FileResponse instance; wikimedia.find_jpeg_url was NOT called.

    Test 2 — test_cache_miss_fetches_saves_inserts_and_returns_file_response:
      Arrange: mock repo.find to return None; mock wikimedia.find_jpeg_url to return "https://upload.wikimedia.org/sample.jpg"; mock an httpx GET to return 200 with b"JPEG_BYTES" as content; mock storage.save to return a Path; mock storage.file_response to return a FileResponse; mock repo.insert to complete without error.
      Act: call await svc.get_or_fetch("toyota", "corolla", 2022).
      Assert: result is a FileResponse instance; storage.save was called once; repo.insert was called once.

    Test 3 — test_concurrent_requests_for_same_key_trigger_exactly_one_wikimedia_fetch:
      Arrange: mock repo.find to return None the first call and return a CacheEntry on subsequent calls (side_effect list); mock wikimedia.find_jpeg_url to return a URL; mock storage.save + storage.file_response + repo.insert as above.
      Act: use asyncio.gather to fire two concurrent calls to svc.get_or_fetch("toyota", "corolla", 2022).
      Assert: wikimedia.find_jpeg_url.call_count == 1 (serialized by per-key lock — second waiter reuses first result).

    Test 4 — test_self_healing_when_db_hit_but_file_absent:
      Arrange: mock repo.find to return a CacheEntry but mock storage.file_response to raise FileNotFoundError on the first call and return a FileResponse on the second call; mock wikimedia.find_jpeg_url to return a URL; mock storage.save + repo.insert as usual.
      Act: call await svc.get_or_fetch("toyota", "corolla", 2022).
      Assert: result is a FileResponse; wikimedia.find_jpeg_url was called once (re-fetch triggered).

    Test 5 — test_raises_http_exception_404_when_no_image_found:
      Arrange: mock repo.find to return None; mock wikimedia.find_jpeg_url to return None.
      Act: call await svc.get_or_fetch("nonexistent", "vehicle", 9999).
      Assert: HTTPException is raised with status_code == 404.

    Use unittest.mock.AsyncMock for all async mocked methods (repo.find, repo.insert, wikimedia.find_jpeg_url, storage.save). Use unittest.mock.MagicMock for sync mocked methods (storage.file_response). Construct the ImageService with Mock objects injected — do not use a real DB or real HTTP client in unit tests.
    Import HTTPException from fastapi. Import pytest. Include from __future__ import annotations.
    No respx fixture needed for unit tests — wikimedia.find_jpeg_url is mocked via AsyncMock, not HTTP-level.

    For Test 2, the image download (httpx.get) is a separate HTTP call inside get_or_fetch (downloading the bytes from the thumburl). This is an internal implementation detail — mock it at the httpx transport level using respx_mock fixture so the URL https://upload.wikimedia.org/sample.jpg returns b"JPEG_BYTES".
  </action>
  <verify>
    <automated>cd /home/ccastro/Projects/carpix && uv run pytest tests/unit/test_image_service.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/carpix_images/services/image_service.py exists, contains class ImageService, __init__ accepting (storage, repo, wikimedia), get_or_fetch raising NotImplementedError, self._locks: dict[tuple[str, str, int], asyncio.Lock] = {}
    - tests/unit/test_image_service.py exists with exactly 5 async test functions
    - All 5 tests FAIL with NotImplementedError (RED gate confirmed)
    - Import: from carpix_images.services import ImageService works without error
    - Commit message: test(05-01): add 5 failing RED tests for ImageService
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → ImageService | Unit test mocks replace real DB/HTTP deps; no external trust surface in Plan 01 |
| ImageService → WikimediaClient | thumburl returned by Wikimedia is treated as a download URL; unvalidated external URL (SSRF vector, addressed in Plan 02 implementation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-I | Information Disclosure | ImageService stub | accept | Stub raises NotImplementedError — no data exposure; tests run in isolated tmp_path with fake bytes only |
| T-05-01-E | Elevation of Privilege | test mock injection | accept | Tests use AsyncMock injected at construction; no real credentials or file paths exercised |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Plan 01; all deps already installed and verified in prior phases |
</threat_model>

<verification>
Run after plan completes:
  uv run pytest tests/unit/test_image_service.py -v
Expected: 5 tests collected, 5 FAILED (NotImplementedError or similar stub failure)
  grep -n "class ImageService" src/carpix_images/services/image_service.py
  grep -n "get_or_fetch" src/carpix_images/services/image_service.py
  grep -n "NotImplementedError" src/carpix_images/services/image_service.py
  python -c "from carpix_images.services import ImageService; print('import OK')"
</verification>

<success_criteria>
- 5 test functions exist in tests/unit/test_image_service.py
- All 5 tests FAIL (RED gate) — NotImplementedError or equivalent stub failure
- ImageService importable from carpix_images.services
- No ruff or mypy --strict errors introduced (stub must be type-annotated)
- Commit created: test(05-01): add 5 failing RED tests for ImageService
</success_criteria>

<output>
Create .planning/phases/05-service-orchestration/05-01-SUMMARY.md when done
</output>

---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/carpix_images/config.py
  - src/carpix_images/domain/normalize.py
  - src/carpix_images/routers/health.py
  - src/carpix_images/main.py
autonomous: true
requirements: [NORM-01, API-03, API-05]
must_haves:
  truths:
    - "canonical_key normalizes brand/model strings to lowercase alphanumeric-only keys"
    - "GET /health returns HTTP 200 even when the database is unreachable"
    - "GET /health body reports db connectivity ('ok'/'error') distinct from process liveness ('status':'ok')"
    - "The app starts via `uvicorn carpix_images.main:app` and /docs is discoverable"
    - "ruff and mypy --strict report zero errors on src/ and tests/"
  artifacts:
    - path: "src/carpix_images/domain/normalize.py"
      provides: "canonical_key() — NORM-01 (verbatim from parent project)"
      contains: "re.sub(r\"[^a-z0-9]\", \"\", value.lower())"
    - path: "src/carpix_images/config.py"
      provides: "Settings(BaseSettings) with database_url; module-level settings singleton"
      contains: "database_url: str"
    - path: "src/carpix_images/routers/health.py"
      provides: "GET /health with asyncpg SELECT 1 probe — API-03, API-05"
      contains: "asyncpg.connect"
    - path: "src/carpix_images/main.py"
      provides: "create_app() factory + module-level app for uvicorn"
      contains: "def create_app() -> FastAPI"
  key_links:
    - from: "src/carpix_images/main.py"
      to: "src/carpix_images/routers/health.py"
      via: "app.include_router(router)"
      pattern: "include_router"
    - from: "src/carpix_images/routers/health.py"
      to: "PostgreSQL"
      via: "asyncpg.connect(dsn) + SELECT 1"
      pattern: "asyncpg\\.connect"
    - from: "src/carpix_images/routers/health.py"
      to: "src/carpix_images/config.py"
      via: "settings.database_url with +asyncpg specifier stripped"
      pattern: "database_url\\.replace"
---

<objective>
Implement the Phase 1 source modules so the Wave 0 failing tests turn GREEN: the canonical_key normalization function (NORM-01), pydantic-settings config (DATABASE_URL only), the /health router with an asyncpg SELECT 1 probe (API-03, API-05), and the FastAPI app factory with a module-level app for uvicorn.

Purpose: Deliver a runnable FastAPI skeleton that satisfies all four Phase 1 success criteria and passes ruff + mypy --strict.
Output: A `uvicorn carpix_images.main:app`-startable service with /health and /docs, full unit suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Contracts the Wave 0 tests (Plan 01) already import. Implement EXACTLY these names/paths. -->

tests import these — names must match exactly:
  carpix_images.domain.normalize.canonical_key(value: str) -> str
  carpix_images.config.Settings   (field: database_url: str)
  carpix_images.config.settings   (module-level singleton)
  carpix_images.routers.health.router        (APIRouter)
  carpix_images.routers.health.asyncpg       (imported so tests patch "...routers.health.asyncpg.connect")
  carpix_images.main.create_app() -> FastAPI
  carpix_images.main.app          (module-level, for `uvicorn carpix_images.main:app`)

Health response contract (D-03, D-04):
  always HTTP 200
  body when DB reachable:   {"status": "ok", "db": "ok"}
  body when DB unreachable: {"status": "ok", "db": "error"}
</interfaces>

<reference_files>
<!-- Read these for verbatim/exact patterns. -->
canonical_key source (copy verbatim, ONLY this function):
  /home/ccastro/Projects/auto-insight-claw/src/auto_insight_claw/domain/vehicle_identity.py  (line 15)
pydantic-settings pattern:
  /home/ccastro/Projects/auto-insight-claw/src/auto_insight_claw/config.py
create_app() factory pattern:
  /home/ccastro/Projects/auto-insight-claw/src/auto_insight_claw/main.py
</reference_files>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical_key and Settings config</name>
  <files>src/carpix_images/domain/normalize.py, src/carpix_images/config.py</files>
  <behavior>
    - canonical_key("Toyota")=="toyota"; ("BYD DM-i")=="byddmi"; ("Land Rover")=="landrover"; ("Alfa-Romeo")=="alfaromeo"; ("Model 3")=="model3"; ("")=="" (NORM-01, from Plan 01 test_normalize.py)
    - Settings() reads DATABASE_URL from env/.env; missing DATABASE_URL raises ValidationError at instantiation (D-05)
    - `from carpix_images.config import settings` succeeds when DATABASE_URL is set (conftest sets it)
  </behavior>
  <read_first>
    - /home/ccastro/Projects/auto-insight-claw/src/auto_insight_claw/domain/vehicle_identity.py (canonical_key verbatim — line 15; do NOT copy clean_display or other functions)
    - /home/ccastro/Projects/auto-insight-claw/src/auto_insight_claw/config.py (Settings/SettingsConfigDict pattern)
    - .planning/phases/01-foundation/01-PATTERNS.md (normalize.py + config.py minimal Phase 1 forms)
    - tests/unit/test_normalize.py (the contract these tests assert — created in Plan 01)
  </read_first>
  <action>
    Create src/carpix_images/domain/normalize.py with `from __future__ import annotations`, `import re`, and the `canonical_key(value: str) -> str` function copied VERBATIM from the parent project: body `return re.sub(r"[^a-z0-9]", "", value.lower())`, including the docstring (NORM-01, D per CONTEXT canonical_refs). Copy ONLY canonical_key — do NOT add clean_display, canonicalize_pair, or normalize_competitors (CONTEXT specifics exclude them from Phase 1).
    Create src/carpix_images/config.py with the pydantic-settings pattern: `from pydantic_settings import BaseSettings, SettingsConfigDict`; class `Settings(BaseSettings)` with `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", case_sensitive=False)` and a single required field `database_url: str` (D-06 — DATABASE_URL only, no Field defaults, no IMAGES_DIR). End with module-level `settings = Settings()` (D-05 — crashes at startup if DATABASE_URL missing).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_normalize.py -x -q && uv run python -c "from carpix_images.config import Settings"</automated>
  </verify>
  <acceptance_criteria>
    - normalize.py contains exactly `return re.sub(r"[^a-z0-9]", "", value.lower())`
    - normalize.py defines only `canonical_key` (no clean_display / canonicalize_pair / normalize_competitors)
    - config.py defines `Settings(BaseSettings)` with `database_url: str` and module-level `settings = Settings()`
    - config.py declares no settings field other than `database_url` (D-06)
    - `uv run pytest tests/unit/test_normalize.py -x` exits 0 (all NORM-01 cases GREEN)
  </acceptance_criteria>
  <done>canonical_key passes all NORM-01 tests; Settings loads DATABASE_URL and exposes the settings singleton.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement /health router and FastAPI app factory</name>
  <files>src/carpix_images/routers/health.py, src/carpix_images/main.py</files>
  <behavior>
    - GET /health returns 200 with {"status":"ok","db":"ok"} when asyncpg.connect succeeds (API-03, API-05)
    - GET /health returns 200 with {"status":"ok","db":"error"} when asyncpg.connect raises (API-03 — never 500)
    - GET /docs returns 200 (routes discoverable; API-03)
    - `uvicorn carpix_images.main:app` resolves a module-level app object
  </behavior>
  <read_first>
    - /home/ccastro/Projects/auto-insight-claw/src/auto_insight_claw/main.py (create_app factory pattern)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 lifespan, Pattern 3 health probe, Pitfall 1 URL scheme, Code Examples)
    - .planning/phases/01-foundation/01-PATTERNS.md (health.py + main.py adaptation notes)
    - tests/unit/test_health.py (patch target + body assertions — created in Plan 01)
    - src/carpix_images/config.py (settings.database_url — created in Task 1)
  </read_first>
  <action>
    Create src/carpix_images/routers/health.py: `import asyncpg`, `from fastapi import APIRouter`, `from carpix_images.config import settings`. Module-level `router = APIRouter()`. Define `async def health() -> dict[str, str]` decorated with `@router.get("/health")`. Inside: start `db_status = "error"`; compute `dsn = settings.database_url.replace("postgresql+asyncpg://", "postgresql://")` (RESEARCH Pitfall 1 — asyncpg rejects the SQLAlchemy `+asyncpg` specifier); in a try block `conn = await asyncpg.connect(dsn, timeout=3)`, `await conn.execute("SELECT 1")`, `await conn.close()`, set `db_status = "ok"`; `except Exception: pass` (catch-all — no stack traces leaked, never raises). Return `{"status": "ok", "db": db_status}` (D-03 status always "ok"; D-04 flat strings). NOTE: verify the asyncpg `connect` timeout kwarg name with `uv run python -c "import asyncpg; help(asyncpg.connect)"` — if `timeout` is not accepted in asyncpg 0.31.0, omit the kwarg (RESEARCH Open Question 1 / A2).
    Create src/carpix_images/main.py: `from contextlib import asynccontextmanager`, `from fastapi import FastAPI`, `from carpix_images.routers.health import router as health_router`. Define a no-op `@asynccontextmanager async def lifespan(app: FastAPI)` that just `yield`s (Phase 3 wires the pool here later — modern lifespan, NOT `@app.on_event`). Define `def create_app() -> FastAPI` that builds `FastAPI(title="carpix-images", version="1.0.0", lifespan=lifespan)`, calls `app.include_router(health_router)`, and returns app. End with module-level `app = create_app()` so `uvicorn carpix_images.main:app` works. Do NOT add middleware or `_validate_startup()` (parent has them; Phase 1 does not need them).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_health.py -x -q && uv run python -c "from carpix_images.main import app; assert any(getattr(r, 'path', None) == '/health' for r in app.routes)"</automated>
  </verify>
  <acceptance_criteria>
    - health.py imports asyncpg at module level (so tests can patch `carpix_images.routers.health.asyncpg.connect`)
    - health.py strips the `+asyncpg` specifier via `.replace("postgresql+asyncpg://", "postgresql://")` before connecting
    - health.py uses `except Exception:` catch-all and always returns `{"status": "ok", "db": <status>}`
    - main.py defines `create_app() -> FastAPI`, uses an `@asynccontextmanager` lifespan (no `@app.on_event`), and ends with module-level `app = create_app()`
    - `uv run pytest tests/unit/test_health.py -x` exits 0 (200/db:ok, 200/db:error, /docs 200 all GREEN)
    - `app.routes` includes a `/health` route
  </acceptance_criteria>
  <done>/health returns 200 with correct db status on both DB-up and DB-down; /docs is reachable; app is importable as carpix_images.main:app.</done>
</task>

<task type="auto">
  <name>Task 3: Quality gate — full suite, ruff, mypy --strict, uvicorn smoke</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (Sampling Rate: per-wave merge command)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pitfall 4 mypy + asyncpg stubs; Environment Availability)
    - pyproject.toml (tool config from Plan 01 — fix here only if a gate fails)
  </read_first>
  <action>
    Run the full Phase 1 quality gate and resolve any failures: (1) `uv run pytest tests/ -x` — entire suite GREEN; (2) `uv run ruff check src/ tests/` — zero lint errors; (3) `uv run ruff format --check src/ tests/` — formatting clean; (4) `uv run mypy src/` — zero errors under strict mode (the asyncpg/pydantic_settings overrides from Plan 01 must suppress missing-stub errors per RESEARCH Pitfall 4). Then a startup smoke check: `uv run uvicorn carpix_images.main:app --help` resolves the app import path without ImportError (full server boot needs a DB but import resolution must succeed). If any gate fails, fix the underlying source/config (e.g., add a missing mypy override, fix an import order for ruff `I`) and re-run — do NOT suppress errors with blanket `# type: ignore` or `# noqa` unless a specific line genuinely warrants it with a reason.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q && uv run ruff check src/ tests/ && uv run mypy src/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -x` exits 0 (full suite GREEN)
    - `uv run ruff check src/ tests/` exits 0 (zero lint errors)
    - `uv run mypy src/` exits 0 (zero errors under strict)
    - `uv run uvicorn carpix_images.main:app --help` resolves the import path without ImportError
    - No new blanket `# type: ignore` / `# noqa` added without an inline reason
  </acceptance_criteria>
  <done>Full unit suite GREEN; ruff and mypy --strict clean on src/ and tests/; app import path resolves for uvicorn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /health | Unauthenticated GET; no user input parsed in Phase 1 |
| /health → PostgreSQL | asyncpg TCP connection using DATABASE_URL credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information disclosure | /health endpoint exception handling | mitigate | `except Exception: pass` — health never returns DB error strings, stack traces, or DSN to the caller; only the flat `db: "error"` token (RESEARCH Security Domain) |
| T-01-04 | Denial of service | asyncpg.connect in /health (hang on unreachable DB) | mitigate | `connect(dsn, timeout=3)` bounds the probe so health checks cannot hang indefinitely; on timeout the catch-all returns 200/`db:error` |
| T-01-05 | Information disclosure | Missing/invalid DATABASE_URL at startup | mitigate | pydantic-settings `Settings()` raises ValidationError at process start (D-05) — fails fast and clearly rather than running with partial/insecure config |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; all deps installed and locked in Plan 01 (uv.lock); RESEARCH Package Legitimacy Audit cleared all packages |
</threat_model>

<verification>
- `uv run pytest tests/ -x` — full suite GREEN (NORM-01, API-03, API-05, /docs)
- `uv run ruff check src/ tests/` — zero errors
- `uv run mypy src/` — zero errors under strict mode
- `uv run uvicorn carpix_images.main:app --help` resolves app import (no ImportError)
- Phase 1 success criteria satisfied: /health 200 always; db field distinct from status; canonical_key matches parent; routes discoverable at /docs
</verification>

<success_criteria>
- canonical_key implemented verbatim and passing all NORM-01 cases (NORM-01)
- GET /health returns 200 with process liveness even when DB is unreachable (API-03)
- GET /health body reports db connectivity ('ok'/'error') distinct from 'status' (API-05)
- App startable via `uvicorn carpix_images.main:app`; /docs discoverable
- ruff and mypy --strict clean on the entire codebase
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>

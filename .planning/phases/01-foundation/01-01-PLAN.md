---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - src/carpix_images/__init__.py
  - src/carpix_images/domain/__init__.py
  - src/carpix_images/routers/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/unit/__init__.py
  - tests/integration/__init__.py
  - tests/unit/test_normalize.py
  - tests/unit/test_health.py
autonomous: true
requirements: [NORM-01, API-03, API-05]
must_haves:
  truths:
    - "uv is installed and on PATH"
    - "uv sync installs all declared dependencies into a project venv"
    - "pytest collects the test suite without ImportError or ValidationError"
    - "Failing unit tests exist for canonical_key, /health, and /docs (RED state — source not yet implemented)"
  artifacts:
    - path: "pyproject.toml"
      provides: "src-layout package config, deps, ruff/mypy/pytest tool config"
      contains: "[tool.pytest.ini_options]"
    - path: "tests/conftest.py"
      provides: "DATABASE_URL env guard before any carpix_images import"
      contains: "os.environ.setdefault"
    - path: "tests/unit/test_normalize.py"
      provides: "Failing NORM-01 tests (parametrized canonical_key)"
      contains: "canonical_key"
    - path: "tests/unit/test_health.py"
      provides: "Failing API-03/API-05 tests (mocked asyncpg)"
      contains: "create_app"
  key_links:
    - from: "tests/conftest.py"
      to: "DATABASE_URL env var"
      via: "os.environ.setdefault before import"
      pattern: "os\\.environ\\.setdefault.*DATABASE_URL"
    - from: "pyproject.toml"
      to: "src/carpix_images"
      via: "hatchling wheel packages + pythonpath"
      pattern: "packages = \\[\"src/carpix_images\"\\]"
---

<objective>
Scaffold the carpix-images project: install uv, create the src-layout package skeleton, author pyproject.toml with all dependencies and tool config (ruff/mypy/pytest), and write the Wave 0 failing test stubs that downstream Plan 02 will make pass.

Purpose: Establish the project structure and a RED test baseline so Plan 02 implements against an executable contract. No source logic is written here — only structure, config, and tests.
Output: A `uv`-managed project that collects tests cleanly, with failing unit tests for canonical_key, /health, and /docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- Source files Plan 02 will create — test stubs here import these. Stubs MUST import the exact names below so RED tests turn GREEN once Plan 02 lands. -->

From src/carpix_images/domain/normalize.py (created in Plan 02):
  def canonical_key(value: str) -> str

From src/carpix_images/main.py (created in Plan 02):
  def create_app() -> FastAPI
  app = create_app()   # module-level, so `uvicorn carpix_images.main:app` works

From src/carpix_images/config.py (created in Plan 02):
  class Settings(BaseSettings)  # field: database_url: str
  settings = Settings()         # module-level singleton

From src/carpix_images/routers/health.py (created in Plan 02):
  router = APIRouter()
  @router.get("/health")  -> dict[str, str]   # asyncpg.connect patched in tests at
                                               # "carpix_images.routers.health.asyncpg.connect"
</interfaces>

<reference_files>
<!-- Parent project analogs — read for exact tool config and conftest pattern. -->
Parent pyproject.toml: /home/ccastro/Projects/auto-insight-claw/pyproject.toml
Parent conftest.py: /home/ccastro/Projects/auto-insight-claw/tests/conftest.py
Parent test style: /home/ccastro/Projects/auto-insight-claw/tests/unit/test_vehicle_identity.py
</reference_files>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install uv and scaffold the src-layout package skeleton</name>
  <files>src/carpix_images/__init__.py, src/carpix_images/domain/__init__.py, src/carpix_images/routers/__init__.py, tests/__init__.py, tests/unit/__init__.py, tests/integration/__init__.py</files>
  <read_first>
    - CLAUDE.md (project constraints: uv, src-layout, package name carpix_images)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 uv, D-02 src-layout)
    - .planning/phases/01-foundation/01-RESEARCH.md (Recommended Project Structure; Pitfall 5 uv install)
  </read_first>
  <action>
    First ensure uv is available: run `command -v uv` — if it is NOT found, install it with `curl -LsSf https://astral.sh/uv/install.sh | sh` and ensure the resulting binary (`$HOME/.local/bin/uv` or `$HOME/.cargo/bin/uv`) is on PATH for subsequent tasks (D-01; RESEARCH Pitfall 5 — uv confirmed absent on this machine).
    Then create the empty package and test `__init__.py` files at exactly these paths: src/carpix_images/__init__.py, src/carpix_images/domain/__init__.py, src/carpix_images/routers/__init__.py, tests/__init__.py, tests/unit/__init__.py, tests/integration/__init__.py. All six are empty files (no imports, no content) — required for Python package discovery and pytest collection. Package name is carpix_images per D-02. tests/integration/__init__.py is a placeholder for Phase 3+.
  </action>
  <verify>
    <automated>command -v uv && test -f src/carpix_images/__init__.py && test -f src/carpix_images/domain/__init__.py && test -f src/carpix_images/routers/__init__.py && test -f tests/__init__.py && test -f tests/unit/__init__.py && test -f tests/integration/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `command -v uv` exits 0 (uv on PATH)
    - All six `__init__.py` files exist at the exact paths listed in files
    - Each `__init__.py` is empty (0 bytes or whitespace-only)
    - No source logic written in this task
  </acceptance_criteria>
  <done>uv is installed and on PATH; all package/test directory __init__.py files exist and are empty.</done>
</task>

<task type="auto">
  <name>Task 2: Author pyproject.toml, .env.example, and sync the environment</name>
  <files>pyproject.toml, .env.example</files>
  <read_first>
    - /home/ccastro/Projects/auto-insight-claw/pyproject.toml (exact ruff/mypy/pytest config to mirror)
    - .planning/phases/01-foundation/01-PATTERNS.md (pyproject.toml adaptation notes — strip parent-specific deps)
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack versions; Pattern 5 pyproject.toml)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-06 DATABASE_URL only)
  </read_first>
  <action>
    Create pyproject.toml for a uv-managed, src-layout package. Use `[build-system]` requires `["hatchling"]`, build-backend `"hatchling.build"`. `[project]` name `carpix-images`, version `0.1.0`, requires-python `>=3.12`. Runtime dependencies (versions per RESEARCH Standard Stack): `fastapi>=0.136.1`, `uvicorn[standard]>=0.47.0`, `pydantic-settings>=2.14.1`, `asyncpg>=0.31.0`. Dev optional-dependencies group `dev`: `pytest>=9.0.3`, `pytest-asyncio>=1.3.0`, `httpx>=0.28.1`, `ruff>=0.15.14`, `mypy>=2.1.0`, `respx>=0.23.1`, `testcontainers[postgres]>=4.14.2`.
    `[tool.hatch.build.targets.wheel]` packages `["src/carpix_images"]`. `[tool.pytest.ini_options]` testpaths `["tests"]`, asyncio_mode `"auto"`, pythonpath `["src"]`. `[tool.mypy]` strict true, python_version `"3.12"`, mypy_path `"src"`, explicit_package_bases true, plugins `["pydantic.mypy"]`. Add `[[tool.mypy.overrides]]` with ignore_missing_imports true for THREE modules only: `["asyncpg", "asyncpg.*"]`, `["pydantic_settings"]`, `["testcontainers", "testcontainers.*"]` — do NOT carry over parent's celery/alembic/langchain/prometheus/fitz overrides. `[tool.ruff]` target-version `"py312"`, line-length 88; `[tool.ruff.lint]` select `["E", "F", "I", "UP"]`.
    Create .env.example containing a single line `DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/carpix` (D-06: DATABASE_URL only; use the `postgresql+asyncpg://` scheme so Phase 3 SQLAlchemy works unchanged — the health router strips the `+asyncpg` specifier itself).
    Then run `uv sync --extra dev` to create the venv and install all deps (lockfile uv.lock is generated).
  </action>
  <verify>
    <automated>uv sync --extra dev && grep -q 'name = "carpix-images"' pyproject.toml && grep -q 'asyncio_mode = "auto"' pyproject.toml && grep -q 'strict = true' pyproject.toml && test -f .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync --extra dev` exits 0 and creates uv.lock
    - pyproject.toml contains `name = "carpix-images"`
    - pyproject.toml contains `packages = ["src/carpix_images"]`
    - pyproject.toml contains exactly three `[[tool.mypy.overrides]]` blocks (asyncpg, pydantic_settings, testcontainers); no celery/alembic/langchain entries
    - pyproject.toml `[tool.ruff.lint]` select equals `["E", "F", "I", "UP"]`
    - .env.example contains `DATABASE_URL=postgresql+asyncpg://`
    - No `IMAGES_DIR` or other forward-declared settings appear anywhere (D-06)
  </acceptance_criteria>
  <done>pyproject.toml is valid, deps install via `uv sync --extra dev`, tool config mirrors the parent project (minus parent-only deps), and .env.example declares DATABASE_URL only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write Wave 0 failing test stubs and conftest env guard</name>
  <files>tests/conftest.py, tests/unit/test_normalize.py, tests/unit/test_health.py</files>
  <behavior>
    - test_normalize.py: canonical_key("Toyota")=="toyota", ("BYD DM-i")=="byddmi", ("Land Rover")=="landrover", ("Alfa-Romeo")=="alfaromeo", ("Model 3")=="model3", ("")=="" (parametrized) — NORM-01
    - test_health.py: GET /health returns 200 with body {"status":"ok","db":"ok"} when asyncpg.connect mock succeeds — API-03, API-05
    - test_health.py: GET /health returns 200 with body {"status":"ok","db":"error"} when asyncpg.connect raises — API-03, API-05
    - test_health.py: GET /docs returns 200 (routes discoverable) — API-03
    - All four/seven tests must FAIL at end of this plan (RED) because source modules do not yet exist — collection must still succeed (ImportError inside a test body is a fail, not a collection error; use module-level imports that resolve only after Plan 02)
  </behavior>
  <read_first>
    - /home/ccastro/Projects/auto-insight-claw/tests/conftest.py (env-guard pattern)
    - /home/ccastro/Projects/auto-insight-claw/tests/unit/test_vehicle_identity.py (class-based, -> None test style)
    - .planning/phases/01-foundation/01-PATTERNS.md (test_normalize.py / test_health.py / conftest.py adaptation notes)
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples: canonical_key tests, health mock tests, conftest guard)
    - .planning/phases/01-foundation/01-VALIDATION.md (Per-Task Verification Map — required test IDs)
  </read_first>
  <action>
    Create tests/conftest.py that runs BEFORE any carpix_images import: `import os` then `os.environ.setdefault("DATABASE_URL", "postgresql+asyncpg://user:pass@localhost:5432/testdb")`. Include a comment explaining Settings() reads env at import time. Set ONLY DATABASE_URL (D-06) — no REDIS_URL or parent-project vars.
    Create tests/unit/test_normalize.py: import `from carpix_images.domain.normalize import canonical_key`. Add a parametrized test covering the six cases in behavior, plus class-based single-assertion tests mirroring the parent style (test_output_is_lowercase, test_dm_i_variants_are_equivalent). Every test function/method has `-> None` return annotation (mypy strict).
    Create tests/unit/test_health.py: import `from unittest.mock import AsyncMock, patch`, `from fastapi.testclient import TestClient`, `from carpix_images.main import create_app`. Write test_health_returns_200_db_ok (patch `"carpix_images.routers.health.asyncpg.connect"` with AsyncMock return, assert 200 and body `{"status":"ok","db":"ok"}`), test_health_returns_200_db_unreachable (patch with side_effect=Exception, assert 200 and body `{"status":"ok","db":"error"}`), and test_docs_accessible (GET /docs == 200). Patch target is where it is USED (routers.health), not where defined. All test functions `-> None`.
    These tests are EXPECTED TO FAIL (RED) because Plan 02 has not yet created the source modules. Do NOT create any source files in src/carpix_images/ in this plan. Confirm collection succeeds (no syntax errors) and tests fail with ImportError/ModuleNotFound on the carpix_images source modules.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/ --collect-only -q 2>&1 | grep -qE 'test_(normalize|health)' && ! uv run pytest tests/unit/ -q 2>&1 | tail -1 | grep -q 'passed'</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py contains `os.environ.setdefault` with `DATABASE_URL` and the `postgresql+asyncpg://` test DSN; no other env vars set
    - tests/unit/test_normalize.py imports `from carpix_images.domain.normalize import canonical_key` and parametrizes the six NORM-01 cases
    - tests/unit/test_health.py patches `"carpix_images.routers.health.asyncpg.connect"` and asserts bodies `{"status":"ok","db":"ok"}` and `{"status":"ok","db":"error"}`
    - tests/unit/test_health.py contains a test asserting `GET /docs` returns 200
    - `uv run pytest tests/unit/ --collect-only` succeeds (no collection/syntax errors)
    - `uv run pytest tests/unit/` reports failures (RED) — no source modules exist yet
    - Every test function/method has a `-> None` return annotation
  </acceptance_criteria>
  <done>conftest env guard set; failing test stubs exist for NORM-01, API-03, API-05, and /docs; pytest collects cleanly and the suite is RED pending Plan 02 implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → local env | uv pulls fastapi/uvicorn/asyncpg/etc. from PyPI; supply-chain trust |
| developer shell → uv installer | `curl | sh` install of uv from astral.sh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | uv-managed PyPI installs (Task 2) | mitigate | All packages cleared by RESEARCH Package Legitimacy Audit (slopcheck 0.6.1, all [OK]); pinned `>=` floors at VERIFIED PyPI versions; uv.lock pins exact resolved versions |
| T-01-01 | Tampering | uv installer via `curl | sh` (Task 1) | accept | Official astral.sh install URL per RESEARCH Pitfall 5; standard distribution channel for uv; no alternative on a machine lacking uv |
| T-01-02 | Information disclosure | .env.example with DATABASE_URL placeholder | mitigate | .env.example contains only placeholder credentials (user:pass) — never real secrets; real .env is git-ignored and created by the operator |
</threat_model>

<verification>
- `uv sync --extra dev` installs all dependencies cleanly
- `uv run pytest tests/unit/ --collect-only` collects with zero errors
- `uv run pytest tests/unit/` runs and reports RED (failures expected — no source yet)
- No file under `src/carpix_images/` contains implementation logic (structure-only; logic is Plan 02)
</verification>

<success_criteria>
- uv installed and on PATH
- pyproject.toml valid, src-layout, deps installed via uv.lock
- All `__init__.py` package/test files exist and are empty
- conftest.py sets DATABASE_URL before imports (D-06: only DATABASE_URL)
- Failing unit tests exist covering NORM-01, API-03, API-05, and /docs discoverability
- Test suite collects cleanly and is RED (Plan 02 turns it GREEN)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>

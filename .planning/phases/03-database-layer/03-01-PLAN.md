---
phase: 03-database-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/carpix_images/infrastructure/__init__.py
  - src/carpix_images/infrastructure/cache_repository.py
  - tests/integration/__init__.py
  - tests/integration/test_cache_repository.py
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
must_haves:
  truths:
    - "Five integration tests exist and fail (RED) because CacheRepository raises NotImplementedError"
    - "sqlalchemy>=2.0 is listed in [project].dependencies in pyproject.toml"
    - "alembic>=1.18 is listed in [project.optional-dependencies].dev in pyproject.toml"
    - "mypy overrides for sqlalchemy.* and alembic.* are present in pyproject.toml"
    - "The infrastructure package exists as a Python package (src/carpix_images/infrastructure/__init__.py)"
    - "tests/integration/ is a Python package (tests/integration/__init__.py already exists — verify only)"
  artifacts:
    - path: "src/carpix_images/infrastructure/__init__.py"
      provides: "Empty package marker for infrastructure package"
    - path: "src/carpix_images/infrastructure/cache_repository.py"
      provides: "CacheEntry dataclass + CacheRepository stub with NotImplementedError on find() and insert()"
    - path: "tests/integration/test_cache_repository.py"
      provides: "5 failing tests: test_migration_creates_table, test_find_returns_entry, test_find_returns_none, test_insert_idempotent, test_pool_lifecycle"
    - path: "pyproject.toml"
      provides: "sqlalchemy>=2.0 in dependencies; alembic>=1.18 in dev; mypy overrides for both"
  key_links:
    - from: "tests/integration/test_cache_repository.py"
      to: "src/carpix_images/infrastructure/cache_repository.py"
      via: "import CacheEntry, CacheRepository"
      pattern: "from carpix_images\\.infrastructure\\.cache_repository import"
---

<objective>
Wave 1 TDD RED baseline: add missing dependencies to pyproject.toml, scaffold the infrastructure package with a stub CacheRepository, and write 5 failing integration tests that define the exact behavior required for DB-01, DB-02, and DB-03. All five tests MUST fail before this plan is complete — a passing test here means the implementation leaked in, which breaks the RED gate.

Purpose: Establish the behavioral contract (tests) before writing production code. Tests define the API surface of CacheRepository and the migration expectation that Wave 2 must satisfy.

Output: pyproject.toml updated; infrastructure package scaffolded; 5 RED integration tests committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-database-layer/03-RESEARCH.md
@.planning/phases/03-database-layer/03-PATTERNS.md

<interfaces>
<!-- Current state of files this plan modifies. -->

From pyproject.toml (lines 9-25, current deps):
```toml
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn[standard]>=0.47.0",
    "pydantic-settings>=2.14.1",
    "asyncpg>=0.31.0",
]

dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "httpx>=0.28.1",
    "ruff>=0.15.14",
    "mypy>=2.1.0",
    "respx>=0.23.1",
    "testcontainers[postgres]>=4.14.2",
]
```

From pyproject.toml (lines 42-53, existing mypy overrides):
```toml
[[tool.mypy.overrides]]
module = ["asyncpg", "asyncpg.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["pydantic_settings"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["testcontainers", "testcontainers.*"]
ignore_missing_imports = true
```

From tests/conftest.py (full file — 7 lines):
```python
import os

os.environ.setdefault(
    "DATABASE_URL", "postgresql+asyncpg://user:pass@localhost:5432/testdb"
)
os.environ.setdefault("IMAGES_DIR", "/tmp/carpix_test_images")
```

tests/integration/__init__.py already exists (empty, confirmed by ls).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sqlalchemy + alembic deps and mypy overrides to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (read current state before editing — lines 1-60 confirmed above, but read again as source of truth before Edit)
  </read_first>
  <behavior>
    - sqlalchemy>=2.0 appears in [project].dependencies list (alongside asyncpg>=0.31.0)
    - alembic>=1.18 appears in [project.optional-dependencies].dev list
    - A [[tool.mypy.overrides]] block for module = ["sqlalchemy", "sqlalchemy.*"] with ignore_missing_imports = true exists after the testcontainers override
    - A [[tool.mypy.overrides]] block for module = ["alembic", "alembic.*"] with ignore_missing_imports = true exists after the sqlalchemy override
  </behavior>
  <action>
    Edit pyproject.toml using the Edit tool:
    1. In [project].dependencies, add "sqlalchemy>=2.0" after the "asyncpg>=0.31.0" line.
    2. In [project.optional-dependencies].dev, add "alembic>=1.18" as the last entry in the list.
    3. After the existing [[tool.mypy.overrides]] block for testcontainers, append two new override blocks:
       - module = ["sqlalchemy", "sqlalchemy.*"] with ignore_missing_imports = true
       - module = ["alembic", "alembic.*"] with ignore_missing_imports = true
    Then run: uv sync
    uv sync installs sqlalchemy and alembic into the venv; the command must exit 0.
  </action>
  <verify>
    <automated>grep -c '"sqlalchemy>=2.0"' pyproject.toml && grep -c '"alembic>=1.18"' pyproject.toml && grep -c '"sqlalchemy\.\*"' pyproject.toml && uv run python -c "import sqlalchemy; import alembic; print('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"sqlalchemy>=2.0"' pyproject.toml returns 1
    - grep -c '"alembic>=1.18"' pyproject.toml returns 1
    - grep -c '"sqlalchemy\.\*"' pyproject.toml returns 1 (the mypy override module pattern)
    - grep -c '"alembic\.\*"' pyproject.toml returns 1
    - uv run python -c "import sqlalchemy; import alembic" exits 0 with no ImportError
  </acceptance_criteria>
  <done>pyproject.toml has both new production and dev deps, two new mypy override blocks; uv sync succeeded; imports work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold infrastructure package + CacheRepository stub + 5 RED integration tests</name>
  <files>
    src/carpix_images/infrastructure/__init__.py,
    src/carpix_images/infrastructure/cache_repository.py,
    tests/integration/test_cache_repository.py
  </files>
  <read_first>
    - tests/integration/__init__.py (verify it exists and is empty — already confirmed, but confirm before writing sibling file)
    - tests/conftest.py (read before modifying; the postgres_container fixture goes in Wave 2, NOT here — Wave 1 tests must fail due to NotImplementedError, not missing fixture)
    - .planning/phases/03-database-layer/03-PATTERNS.md section "cache_repository.py" and "test_cache_repository.py" for exact code shape
  </read_first>
  <behavior>
    CacheRepository stub (cache_repository.py):
    - File starts with: from __future__ import annotations
    - Imports: dataclass from dataclasses, datetime from datetime, AsyncEngine from sqlalchemy.ext.asyncio
    - CacheEntry dataclass has fields: brand_key: str, model_key: str, year: int, local_path: str, source_url: str, file_title: str, cached_at: datetime
    - CacheRepository.__init__(self, engine: AsyncEngine) -> None stores self._engine = engine
    - CacheRepository.find(self, brand_key: str, model_key: str, year: int) -> CacheEntry | None raises NotImplementedError
    - CacheRepository.insert(self, brand_key: str, model_key: str, year: int, local_path: str, source_url: str, file_title: str) -> None raises NotImplementedError

    Integration tests (test_cache_repository.py):
    - pytestmark = pytest.mark.usefixtures("postgres_container") at module level
    - run_migrations is a SYNC def fixture (scope="module", autouse=True) — not async def
    - engine fixture is async with yield e / await e.dispose() teardown
    - repo fixture yields CacheRepository(engine)
    - test_migration_creates_table() is sync (calls asyncio.run(_table_exists(...)))
    - test_find_returns_entry, test_find_returns_none, test_insert_idempotent, test_pool_lifecycle are async def
    - All five tests fail because CacheRepository raises NotImplementedError and alembic migration files do not exist yet

    RED gate: ALL five tests must fail when run. If any pass, the plan is not complete.
  </behavior>
  <action>
    Step 1 — Create src/carpix_images/infrastructure/__init__.py as an empty file (empty package marker).

    Step 2 — Create src/carpix_images/infrastructure/cache_repository.py with:
    - from __future__ import annotations at top
    - Imports: dataclass, datetime, text (from sqlalchemy), AsyncEngine (from sqlalchemy.ext.asyncio)
    - CacheEntry dataclass with 7 fields matching the vehicle_images table columns (brand_key: str, model_key: str, year: int, local_path: str, source_url: str, file_title: str, cached_at: datetime)
    - CacheRepository class: __init__ stores self._engine: AsyncEngine
    - find() signature: async def find(self, brand_key: str, model_key: str, year: int) -> CacheEntry | None — raises NotImplementedError
    - insert() signature: async def insert(self, brand_key: str, model_key: str, year: int, local_path: str, source_url: str, file_title: str) -> None — raises NotImplementedError

    Step 3 — Create tests/integration/test_cache_repository.py following the full structure from PATTERNS.md "Carpix full test file structure":
    - Module docstring listing DB-01, DB-02, DB-03
    - from __future__ import annotations
    - Imports: asyncio, os, asyncpg, pytest, alembic.command, alembic.config.Config, sqlalchemy.ext.asyncio.create_async_engine, CacheEntry and CacheRepository from carpix_images.infrastructure.cache_repository
    - pytestmark = pytest.mark.usefixtures("postgres_container")
    - Helper functions: _alembic_cfg() -> Config (uses Config("alembic.ini") + set_main_option); _asyncpg_url() -> str (strips postgresql+asyncpg:// prefix); async _table_exists(url, table) -> bool (asyncpg raw connect, information_schema query)
    - Fixtures: run_migrations (sync def, scope="module", autouse=True, calls command.upgrade); engine (async, yields create_async_engine result, disposes in teardown); repo (sync, yields CacheRepository(engine))
    - 5 test functions with -> None return type annotations

    Do NOT add the postgres_container fixture to tests/conftest.py yet — that is Wave 2 work. The tests fail in Wave 1 because (a) the fixture is missing, (b) CacheRepository raises NotImplementedError, and (c) alembic migration files do not exist. All three are correct RED conditions.

    Commit message format: test(03-01): add RED baseline — CacheRepository stub + 5 failing integration tests
  </action>
  <verify>
    <automated>uv run mypy src/carpix_images/infrastructure/cache_repository.py --strict && uv run ruff check src/carpix_images/infrastructure/cache_repository.py tests/integration/test_cache_repository.py && uv run pytest tests/integration/test_cache_repository.py -x -q 2>&1 | grep -E "ERROR|FAILED|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - uv run mypy src/carpix_images/infrastructure/cache_repository.py --strict exits 0 (no type errors on the stub)
    - uv run ruff check src/carpix_images/infrastructure/cache_repository.py exits 0
    - uv run ruff check tests/integration/test_cache_repository.py exits 0
    - uv run pytest tests/integration/test_cache_repository.py -q shows 0 passed, at least 5 ERROR or FAILED (RED gate confirmed)
    - src/carpix_images/infrastructure/__init__.py exists (stat confirms)
    - grep -c "class CacheRepository" src/carpix_images/infrastructure/cache_repository.py returns 1
    - grep -c "NotImplementedError" src/carpix_images/infrastructure/cache_repository.py returns 2 (one per method)
    - grep -c "pytestmark" tests/integration/test_cache_repository.py returns 1
    - grep -c "def run_migrations" tests/integration/test_cache_repository.py returns 1 (sync def, not async def)
  </acceptance_criteria>
  <done>Infrastructure package exists; CacheRepository stub raises NotImplementedError; 5 integration tests are written, ruff/mypy clean, and ALL FAIL (RED). Unit tests still pass: uv run pytest tests/unit/ -q exits green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → CacheRepository stub | Test code imports and instantiates the stub; no trust concern at this phase (stubs only raise NotImplementedError) |
| pyproject.toml → PyPI registry | Package install pulls sqlalchemy and alembic from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | sqlalchemy>=2.0 install | accept | Package Legitimacy Audit completed in RESEARCH.md: slopcheck OK, 100M+/mo downloads, 20yr history — no checkpoint needed |
| T-03-02 | Tampering | alembic>=1.18 install | accept | Package Legitimacy Audit completed in RESEARCH.md: slopcheck OK, 12yr history, same SQLAlchemy org — no checkpoint needed |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | All installs verified via Package Legitimacy Audit in RESEARCH.md (all [VERIFIED], none [ASSUMED] or [SUS]) |
</threat_model>

<verification>
RED gate check: after both tasks, run:

```bash
uv run pytest tests/unit/ -q        # must be all green (existing tests unaffected)
uv run pytest tests/integration/ -q # must show 0 passed, ≥5 ERROR/FAILED
```

If any integration test passes, the RED gate is broken — investigate and ensure CacheRepository stubs raise NotImplementedError and alembic migration files are absent.
</verification>

<success_criteria>
- pyproject.toml: sqlalchemy>=2.0 in production deps; alembic>=1.18 in dev deps; two new mypy override blocks
- uv sync succeeded; sqlalchemy and alembic importable in project venv
- src/carpix_images/infrastructure/__init__.py exists (empty)
- src/carpix_images/infrastructure/cache_repository.py: CacheEntry dataclass + CacheRepository with find() and insert() raising NotImplementedError
- tests/integration/test_cache_repository.py: 5 tests, ruff + mypy clean, ALL 5 FAIL
- Existing unit tests (tests/unit/) remain green
</success_criteria>

<output>
Create .planning/phases/03-database-layer/03-01-SUMMARY.md when done
</output>
